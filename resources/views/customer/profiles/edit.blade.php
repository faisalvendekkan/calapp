<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between gap-4"><h2 class="text-2xl font-bold text-slate-950">Edit {{ $profile->name }}</h2>@if ($profile->status === 'published')<a href="{{ route('public.profile', $profile->slug) }}" target="_blank" class="text-sm font-bold text-blue-700">Open public profile →</a>@endif</div></x-slot>
    <div class="py-10"><div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">@if (session('status'))<div class="mb-6 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-emerald-800">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('profiles.update', $profile) }}" class="rounded-3xl bg-white p-6 shadow-sm sm:p-10">@csrf @method('PUT') @include('customer.profiles.partials.form', ['profile' => $profile])<div class="mt-8 flex flex-wrap gap-3"><button class="rounded-xl bg-blue-700 px-6 py-3 font-bold text-white hover:bg-blue-800">Save profile</button><a href="{{ route('profiles.index') }}" class="rounded-xl border border-slate-300 px-6 py-3 font-bold text-slate-700">Back</a></div></form></div></div>
</x-app-layout>
